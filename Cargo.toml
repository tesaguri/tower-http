[package]
name = "tower-http"
version = "0.1.0"
authors = ["Carl Lerche <me@carllerche.com>"]

[workspace]
members = [
  "tower-add-origin",
  "tower-compress",
]

[dependencies]
tower-add-origin = { path = "tower-add-origin" }
tower-compress = { path = "tower-compress" }
